[package]
name = "canvas-layer"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Canvas Layer demo — a CanvasLayer keeps HUD elements fixed on screen"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

[package]
name = "ui-health-bar"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "UI Health Bar demo — drives a ProgressBar and Label from Rust, exposing"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

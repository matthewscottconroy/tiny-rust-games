[package]
name = "callable-deferred"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Callable Deferred demo — call_deferred for safe scene-tree mutation from"

[lib]
name = "callable_deferred"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

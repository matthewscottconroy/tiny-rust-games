[package]
name = "signals"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Signals GDExtension demo — Rust-defined signals and typed emission"

[lib]
name = "signals"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

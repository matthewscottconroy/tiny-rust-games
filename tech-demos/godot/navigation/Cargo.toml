[package]
name = "navigation"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Navigation GDExtension demo — NavigationAgent2D path-following from Rust"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = { version = "0.5", features = ["experimental-godot-api"] }

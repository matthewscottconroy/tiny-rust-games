[package]
name = "player-movement"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Player Movement GDExtension demo — CharacterBody2D with WASD/arrow-key movement,"

[lib]
name = "player_movement"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

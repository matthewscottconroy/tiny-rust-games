[package]
name = "camera-follow"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Camera Follow demo — a Camera2D that smoothly lerps toward the first node"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

[package]
name = "object-pool"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Object Pool demo — pre-spawned nodes recycled via visibility and"

[lib]
name = "object_pool"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

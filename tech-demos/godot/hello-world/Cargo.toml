[package]
name = "hello-world"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Hello World GDExtension demo — the minimum viable Rust extension for Godot 4"

[lib]
name = "hello_world"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

[package]
name = "spawner"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Spawner GDExtension demo — demonstrates dynamic node creation from Rust"

[lib]
name = "spawner"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

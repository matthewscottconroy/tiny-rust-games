[package]
name = "groups"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Groups GDExtension demo — demonstrates Godot node groups from Rust"

[lib]
name = "groups"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

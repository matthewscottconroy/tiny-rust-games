[package]
name = "timer-and-countdown"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Timer and Countdown demo — creates a repeating Timer child, tracks ticks,"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

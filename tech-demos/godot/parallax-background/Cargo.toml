[package]
name = "parallax-background"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Parallax Background GDExtension demo — multiple ParallaxLayer nodes driven"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

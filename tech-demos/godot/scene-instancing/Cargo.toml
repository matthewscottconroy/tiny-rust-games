[package]
name = "scene-instancing"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Scene Instancing GDExtension demo — load a .tscn at runtime and instantiate"

[lib]
name = "scene_instancing"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

[package]
name = "scene-tree"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Scene Tree GDExtension demo — navigating the Godot scene tree from Rust"

[lib]
name = "scene_tree"
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

[package]
name = "tooltip-system"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Tooltip System demo — shows a PanelContainer with tooltip text on mouse"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

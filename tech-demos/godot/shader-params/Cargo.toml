[package]
name = "shader-params"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "Shader Params demo — drives a ShaderMaterial's uniform parameters from Rust,"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
godot = "0.5"

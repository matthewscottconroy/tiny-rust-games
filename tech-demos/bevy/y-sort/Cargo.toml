[package]
name = "y-sort"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Y-sort (depth sorting) — a reusable Bevy plugin"

[dependencies]
bevy.workspace = true

[lints]
workspace = true

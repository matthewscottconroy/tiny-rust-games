[package]
name = "ecs-ascii-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ECS ASCII-grid NPC simulation — a reusable, headless Bevy plugin"

[dependencies]
bevy.workspace = true

[lints]
workspace = true

[package]
name = "combo-system"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Combo system — a reusable Bevy plugin for input-buffer sequence matching"

[dependencies]
bevy.workspace = true

[lints]
workspace = true

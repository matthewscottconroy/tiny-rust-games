[package]
name = "save-load"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Save / load — a reusable Bevy plugin persisting a resource to disk as JSON"

[dependencies]
bevy.workspace = true
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true

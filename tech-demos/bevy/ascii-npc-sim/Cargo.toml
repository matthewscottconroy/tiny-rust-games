[package]
name = "ascii-npc-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ASCII NPC simulation — a reusable, headless Bevy plugin"

[dependencies]
bevy.workspace = true
rand = "0.8"

[lints]
workspace = true

[package]
name = "minimap"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Minimap demo — two Camera2d instances in one window, as a reusable plugin"

[dependencies]
bevy.workspace = true

[lints]
workspace = true

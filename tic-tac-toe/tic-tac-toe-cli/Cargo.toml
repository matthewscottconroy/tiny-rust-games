[package]
name = "tic-tac-toe-cli"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/matthewscottconroy/tiny-rust-games"
description = "An interactive terminal frontend for tic_tac_toe_lib"

[dependencies]
tic-tac-toe-lib = { path = "../tic-tac-toe-lib" }

# Public items must carry `///` docs; CI runs clippy with `-D warnings`.
[lints.rust]
missing_docs = "warn"
